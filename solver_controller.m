function dydt = solver(y0, bromsKraft, trumRadie)
    %Initierar startvärden
    h = y0(1); %Höjd (för själva hissen)
    v = y0(2); %Hastighet (positivt antingen upp eller ner, vi får bestämma oss)
    varme = y0(3); %Värmeenergiutveckling under processen
end